23
* maven-jar-plugin automatically inserts ${module.name} as Automatic-Module-Name.
 If ${module.name} is not defined, it will use ${project.groupId}.${project.artifactId} with - replaced with _

19
* Moving Changelog to plain text file at project root.
* Switching to release-plugin 2.5 to support git 1.8.

15
* Fixed failing site generation and deployment.

14
*Added creation of source jar during verify phase to ensure this is used during install and deploy phases.

13
* Migrated to GitHub.
